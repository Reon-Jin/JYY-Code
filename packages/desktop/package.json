{
  "$schema": "https://json.schemastore.org/package.json",
  "name": "@jyycode-ai/desktop",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun run stage:sidecar && tauri dev",
    "build": "bun run stage:sidecar && tauri build",
    "stage:sidecar": "bun script/stage-sidecar.ts",
    "test": "bun test script",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@tauri-apps/cli": "catalog:",
    "@types/bun": "catalog:",
    "@types/node": "catalog:",
    "typescript": "catalog:"
  }
}
