{
  "$schema": "https://json.schemastore.org/package.json",
  "name": "@jyycode-ai/desktop",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun run stage:sidecar --dev && bun run --bun tauri dev",
    "build": "bun run stage:sidecar && bun script/build-desktop.ts",
    "stage:sidecar": "bun script/stage-sidecar.ts",
    "smoke:macos": "bash script/smoke-macos.sh",
    "test": "bun test script",
    "typecheck": "bun run --bun tsc --noEmit"
  },
  "devDependencies": {
    "@tauri-apps/cli": "catalog:",
    "@types/bun": "catalog:",
    "@types/node": "catalog:",
    "typescript": "catalog:"
  }
}
