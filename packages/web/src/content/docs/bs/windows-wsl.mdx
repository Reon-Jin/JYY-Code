---
title: Windows (WSL)
description: Pokrenite JYYCode na Windowsu preko WSL-a.
---

import { Steps } from "@astrojs/starlight/components"

Iako JYYCode moze raditi direktno na Windowsu, preporucujemo [Windows Subsystem for Linux (WSL)](https://learn.microsoft.com/en-us/windows/wsl/install) za najbolje iskustvo. WSL daje Linux okruzenje koje glatko radi sa JYYCode funkcijama.

:::tip[Zašto WSL?]
WSL nudi bolje performanse fajl sistema, punu terminalsku podrsku i kompatibilnost s razvojnim alatima na koje se JYYCode oslanja.
:::

---

## Postavljanje

<Steps>

1. **Instalirajte WSL**

   Ako vec niste, [instalirajte WSL](https://learn.microsoft.com/en-us/windows/wsl/install) prema zvanicnom Microsoft vodicu.

2. **Instalirajte JYYCode u WSL-u**

   Kad je WSL spreman, otvorite WSL terminal i instalirajte JYYCode jednom od [metoda instalacije](/docs/).

   ```bash
   curl -fsSL https://jyycode.ai/install | bash
   ```

3. **Koristite JYYCode iz WSL-a**

   Idite u direktorij projekta (Windows fajlovima pristupate preko `/mnt/c/`, `/mnt/d/` itd.) i pokrenite JYYCode.

   ```bash
   cd /mnt/c/Users/YourName/project
   jyycode
   ```

</Steps>

---

## Desktop aplikacija + WSL Server

Ako preferirate JYYCode Desktop aplikaciju, ali zelite da server radi u WSL-u:

1. **Pokrenite server u WSL-u** sa `--hostname 0.0.0.0` da dozvolite vanjske konekcije:

   ```bash
   jyycode serve --hostname 0.0.0.0 --port 4096
   ```

2. **Povezite Desktop aplikaciju** na `http://localhost:4096`

:::note
Ako `localhost` ne radi u vasem setupu, povezte se preko WSL IP adrese (u WSL-u: `hostname -I`) i koristite `http://<wsl-ip>:4096`.
:::

:::caution
Kada koristite `--hostname 0.0.0.0`, postavite `JYYCODE_SERVER_PASSWORD` da zastitite server.

```bash
JYYCODE_SERVER_PASSWORD=your-password jyycode serve --hostname 0.0.0.0
```

:::

---

## Web klijent + WSL

Za najbolje web iskustvo na Windowsu:

1. **Pokrenite `jyycode web` u WSL terminalu** umjesto u PowerShell-u:

   ```bash
   jyycode web --hostname 0.0.0.0
   ```

2. **Otvorite iz Windows browsera** na `http://localhost:<port>` (JYYCode ispisuje URL)

Pokretanje `jyycode web` iz WSL-a osigurava ispravan pristup fajl sistemu i terminalsku integraciju, a i dalje je dostupno iz Windows browsera.

---

## Pristup Windows fajlovima

WSL moze pristupiti svim Windows fajlovima kroz `/mnt/` direktorij:

- Disk `C:` → `/mnt/c/`
- Disk `D:` → `/mnt/d/`
- I tako dalje...

Primjer:

```bash
cd /mnt/c/Users/YourName/Documents/project
jyycode
```

:::tip
Za najgladje iskustvo, razmislite da klonirate/kopirate repo u WSL fajl sistem (npr. pod `~/code/`) i tu pokrenete JYYCode.
:::

---

## Savjeti

- Drzite JYYCode u WSL-u za projekte na Windows diskovima - pristup fajlovima je jednostavan
- Koristite VS Code [WSL ekstenziju](https://code.visualstudio.com/docs/remote/wsl) uz JYYCode za integrisan tok rada
- Vase JYYCode konfiguracije i sesije cuvaju se u WSL okruzenju na `~/.local/share/jyycode/`
