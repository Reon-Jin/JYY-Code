---
title: CLI
description: Opciones y comandos de la CLI de JYYCode.
---

import { Tabs, TabItem } from "@astrojs/starlight/components"

La CLI de JYYCode de forma predeterminada inicia el [TUI](/docs/tui) cuando se ejecuta sin ningún argumento.

```bash
jyycode
```

Pero también acepta comandos como se documenta en esta página. Esto le permite interactuar con JYYCode mediante programación.

```bash
jyycode run "Explain how closures work in JavaScript"
```

---

### tui

Inicie la interfaz de usuario del terminal JYYCode.

```bash
jyycode [project]
```

#### Opciones

| Opción                                   | Corta | Descripción                                                           |
| ---------------------------------------- | ----- | --------------------------------------------------------------------- |
| <nobr><code>{"--continue"}</code></nobr> | `-c`  | Continuar la última sesión                                            |
| <nobr><code>{"--session"}</code></nobr>  | `-s`  | ID de sesión para continuar                                           |
| <nobr><code>{"--fork"}</code></nobr>     |       | Bifurcar la sesión al continuar (usar con `--continue` o `--session`) |
| <nobr><code>{"--prompt"}</code></nobr>   |       | Aviso de uso                                                          |
| <nobr><code>{"--model"}</code></nobr>    | `-m`  | Modelo a utilizar en forma de proveedor/modelo                        |
| <nobr><code>{"--agent"}</code></nobr>    |       | Agente a utilizar                                                     |
| <nobr><code>{"--port"}</code></nobr>     |       | Puerto para escuchar                                                  |
| <nobr><code>{"--hostname"}</code></nobr> |       | Nombre de host para escuchar                                          |

---

## Comandos

La CLI de JYYCode también tiene los siguientes comandos.

---

### agent

Administrar agentes para JYYCode.

```bash
jyycode agent [command]
```

---

### attach

Conecte una terminal a un servidor backend JYYCode que ya se esté ejecutando y iniciado mediante los comandos `serve` o `web`.

```bash
jyycode attach [url]
```

Esto permite usar TUI con un backend remoto JYYCode. Por ejemplo:

```bash
# Start the backend server for web/mobile access
jyycode web --port 4096 --hostname 0.0.0.0

# In another terminal, attach the TUI to the running backend
jyycode attach http://10.20.30.40:4096
```

#### Opciones

| Opción                                   | Corta | Descripción                                                                               |
| ---------------------------------------- | ----- | ----------------------------------------------------------------------------------------- |
| <nobr><code>{"--dir"}</code></nobr>      |       | Directorio de trabajo para iniciar TUI en                                                 |
| <nobr><code>{"--continue"}</code></nobr> | `-c`  | Continuar la última sesión                                                                |
| <nobr><code>{"--session"}</code></nobr>  | `-s`  | ID de sesión para continuar                                                               |
| <nobr><code>{"--fork"}</code></nobr>     |       | Bifurcar la sesión al continuar (usar con `--continue` o `--session`)                     |
| <nobr><code>{"--password"}</code></nobr> | `-p`  | Contraseña de autenticación básica (predeterminada: `JYYCODE_SERVER_PASSWORD`)           |
| <nobr><code>{"--username"}</code></nobr> | `-u`  | Usuario de autenticación básica (predeterminado: `JYYCODE_SERVER_USERNAME` u `jyycode`) |

---

#### create

Cree un nuevo agente con configuración personalizada.

```bash
jyycode agent create
```

Este comando lo guiará en la creación de un nuevo agente con un mensaje del sistema personalizado y una configuración de herramientas.

---

#### list

Enumere todos los agentes disponibles.

```bash
jyycode agent list
```

---

### auth

Comando para administrar credenciales e iniciar sesión para proveedores.

```bash
jyycode auth [command]
```

---

#### login

JYYCode funciona con la lista de proveedores en [Models.dev](https://models.dev), por lo que puede usar `jyycode auth login` para configurar las claves API para cualquier proveedor que desee utilizar. Esto se almacena en `~/.local/share/jyycode/auth.json`.

```bash
jyycode auth login
```

Cuando se inicia JYYCode, carga los proveedores desde el archivo de credenciales. Y si hay claves definidas en sus entornos o un archivo `.env` en su proyecto.

---

#### list

Enumera todos los proveedores autenticados tal como están almacenados en el archivo de credenciales.

```bash
jyycode auth list
```

O la versión corta.

```bash
jyycode auth ls
```

---

#### logout

Cierra tu sesión de un proveedor eliminándolo del archivo de credenciales.

```bash
jyycode auth logout
```

---

### github

Administre el agente GitHub para la automatización del repositorio.

```bash
jyycode github [command]
```

---

#### install

Instale el agente GitHub en su repositorio.

```bash
jyycode github install
```

Esto configura el flujo de trabajo de acciones GitHub necesario y lo guía a través del proceso de configuración. [Más información](/docs/github).

---

#### run

Ejecute el agente GitHub. Esto se usa normalmente en acciones GitHub.

```bash
jyycode github run
```

##### Opciones

| Opción                                | Descripción                                    |
| ------------------------------------- | ---------------------------------------------- |
| <nobr><code>{"--event"}</code></nobr> | GitHub evento simulado para ejecutar el agente |
| <nobr><code>{"--token"}</code></nobr> | GitHub token de acceso personal                |

---

### mcp

Administrar servidores de protocolo de contexto modelo.

```bash
jyycode mcp [command]
```

---

#### add

Agregue un servidor MCP a su configuración.

```bash
jyycode mcp add
```

Este comando lo guiará para agregar un servidor MCP local o remoto.

---

#### list

Enumere todos los servidores MCP configurados y su estado de conexión.

```bash
jyycode mcp list
```

O utilice la versión corta.

```bash
jyycode mcp ls
```

---

#### auth

Autentíquese con un servidor MCP habilitado para OAuth.

```bash
jyycode mcp auth [name]
```

Si no proporciona un nombre de servidor, se le pedirá que seleccione entre los servidores compatibles con OAuth disponibles.

También puede enumerar los servidores compatibles con OAuth y su estado de autenticación.

```bash
jyycode mcp auth list
```

O utilice la versión corta.

```bash
jyycode mcp auth ls
```

---

#### logout

Elimine las credenciales OAuth para un servidor MCP.

```bash
jyycode mcp logout [name]
```

---

#### debug

Depurar problemas de conexión OAuth para un servidor MCP.

```bash
jyycode mcp debug <name>
```

---

### models

Enumere todos los modelos disponibles de los proveedores configurados.

```bash
jyycode models [provider]
```

Este comando muestra todos los modelos disponibles en sus proveedores configurados en el formato `provider/model`.

Esto es útil para determinar el nombre exacto del modelo que se usará en [su configuración](/docs/config/).

Opcionalmente, puede pasar un ID de proveedor para filtrar modelos por ese proveedor.

```bash
jyycode models anthropic
```

#### Opciones

| Opción                                  | Descripción                                                                 |
| --------------------------------------- | --------------------------------------------------------------------------- |
| <nobr><code>{"--refresh"}</code></nobr> | Actualizar la caché de modelos desde models.dev                             |
| <nobr><code>{"--verbose"}</code></nobr> | Utilice una salida del modelo más detallada (incluye metadatos como costos) |

Utilice el indicador `--refresh` para actualizar la lista de modelos almacenados en caché. Esto es útil cuando se han agregado nuevos modelos a un proveedor y desea verlos en JYYCode.

```bash
jyycode models --refresh
```

---

### run

Ejecute jyycode en modo no interactivo pasando un mensaje directamente.

```bash
jyycode run [message..]
```

Esto es útil para secuencias de comandos, automatización o cuando desea una respuesta rápida sin iniciar el TUI completo. Por ejemplo.

```bash "jyycode run"
jyycode run Explain the use of context in Go
```

También puede conectarse a una instancia `jyycode serve` en ejecución para evitar tiempos de arranque en frío del servidor MCP en cada ejecución:

```bash
# Start a headless server in one terminal
jyycode serve

# In another terminal, run commands that attach to it
jyycode run --attach http://localhost:4096 "Explain async/await in JavaScript"
```

#### Opciones

| Opción                                   | Corta | Descripción                                                                               |
| ---------------------------------------- | ----- | ----------------------------------------------------------------------------------------- |
| <nobr><code>{"--command"}</code></nobr>  |       | El comando a ejecutar, use mensaje para args                                              |
| <nobr><code>{"--continue"}</code></nobr> | `-c`  | Continuar la última sesión                                                                |
| <nobr><code>{"--session"}</code></nobr>  | `-s`  | ID de sesión para continuar                                                               |
| <nobr><code>{"--fork"}</code></nobr>     |       | Bifurcar la sesión al continuar (usar con `--continue` o `--session`)                     |
| <nobr><code>{"--share"}</code></nobr>    |       | Comparte la sesión                                                                        |
| <nobr><code>{"--model"}</code></nobr>    | `-m`  | Modelo a utilizar en forma de proveedor/modelo                                            |
| <nobr><code>{"--agent"}</code></nobr>    |       | Agente a utilizar                                                                         |
| <nobr><code>{"--file"}</code></nobr>     | `-f`  | Archivo(s) para adjuntar al mensaje                                                       |
| <nobr><code>{"--format"}</code></nobr>   |       | Formato: predeterminado (formateado) o json (eventos JSON sin formato)                    |
| <nobr><code>{"--title"}</code></nobr>    |       | Título de la sesión (utiliza un mensaje truncado si no se proporciona ningún valor)       |
| <nobr><code>{"--attach"}</code></nobr>   |       | Adjuntar a un servidor jyycode en ejecución (por ejemplo, http://localhost:4096)         |
| <nobr><code>{"--password"}</code></nobr> | `-p`  | Contraseña de autenticación básica (predeterminada: `JYYCODE_SERVER_PASSWORD`)           |
| <nobr><code>{"--username"}</code></nobr> | `-u`  | Usuario de autenticación básica (predeterminado: `JYYCODE_SERVER_USERNAME` u `jyycode`) |
| <nobr><code>{"--dir"}</code></nobr>      |       | Directorio de ejecución, o ruta en el servidor remoto al adjuntar                         |
| <nobr><code>{"--variant"}</code></nobr>  |       | Variante del modelo (esfuerzo de razonamiento específico del proveedor)                   |
| <nobr><code>{"--thinking"}</code></nobr> |       | Mostrar bloques de pensamiento                                                            |
| <nobr><code>{"--port"}</code></nobr>     |       | Puerto para el servidor local (el puerto predeterminado es aleatorio)                     |

---

### serve

Inicie un servidor JYYCode sin cabeza para acceso API. Consulte los [documentos del servidor](/docs/server) para conocer la interfaz HTTP completa.

```bash
jyycode serve
```

Esto inicia un servidor HTTP que proporciona acceso API a la funcionalidad jyycode sin la interfaz TUI. Configure `JYYCODE_SERVER_PASSWORD` para habilitar la autenticación básica HTTP (el nombre de usuario predeterminado es `jyycode`).

#### Opciones

| Opción                                   | Descripción                                          |
| ---------------------------------------- | ---------------------------------------------------- |
| <nobr><code>{"--port"}</code></nobr>     | Puerto para escuchar                                 |
| <nobr><code>{"--hostname"}</code></nobr> | Nombre de host para escuchar                         |
| <nobr><code>{"--mdns"}</code></nobr>     | Habilitar el descubrimiento de mDNS                  |
| <nobr><code>{"--cors"}</code></nobr>     | Orígenes de navegador adicionales para permitir CORS |

---

### session

Administrar JYYCode sesiones.

```bash
jyycode session [command]
```

---

#### list

Enumere todas las sesiones JYYCode.

```bash
jyycode session list
```

##### Opciones

| Opción                                    | Corta | Descripción                             |
| ----------------------------------------- | ----- | --------------------------------------- |
| <nobr><code>{"--max-count"}</code></nobr> | `-n`  | Limitar a N sesiones más recientes      |
| <nobr><code>{"--format"}</code></nobr>    |       | Formato de salida: tabla o json (tabla) |

---

### stats

Muestre el uso de tokens y las estadísticas de costos para sus sesiones JYYCode.

```bash
jyycode stats
```

#### Opciones

| Opción                                  | Descripción                                                                                                              |
| --------------------------------------- | ------------------------------------------------------------------------------------------------------------------------ |
| <nobr><code>{"--days"}</code></nobr>    | Mostrar estadísticas de los últimos N días (todo el tiempo)                                                              |
| <nobr><code>{"--tools"}</code></nobr>   | Número de herramientas para mostrar (todas)                                                                              |
| <nobr><code>{"--models"}</code></nobr>  | Mostrar el desglose del uso del modelo (oculto de forma predeterminada). Pase un número para mostrar la parte superior N |
| <nobr><code>{"--project"}</code></nobr> | Filtrar por proyecto (todos los proyectos, cadena vacía: proyecto actual)                                                |

---

### export

Exportar datos de la sesión como JSON.

```bash
jyycode export [sessionID]
```

Si no proporciona una ID de sesión, se le pedirá que seleccione entre las sesiones disponibles.

---

### import

Importe datos de sesión desde un archivo JSON o una URL compartida JYYCode.

```bash
jyycode import <file>
```

Puede importar desde un archivo local o una URL compartida JYYCode.

```bash
jyycode import session.json
jyycode import https://opncd.ai/s/abc123
```

---

### web

Inicie un servidor JYYCode sin cabeza con una interfaz web.

```bash
jyycode web
```

Esto inicia un servidor HTTP y abre un navegador web para acceder a JYYCode a través de una interfaz web. Configure `JYYCODE_SERVER_PASSWORD` para habilitar la autenticación básica HTTP (el nombre de usuario predeterminado es `jyycode`).

#### Opciones

| Opción                                   | Descripción                                          |
| ---------------------------------------- | ---------------------------------------------------- |
| <nobr><code>{"--port"}</code></nobr>     | Puerto para escuchar                                 |
| <nobr><code>{"--hostname"}</code></nobr> | Nombre de host para escuchar                         |
| <nobr><code>{"--mdns"}</code></nobr>     | Habilitar el descubrimiento de mDNS                  |
| <nobr><code>{"--cors"}</code></nobr>     | Orígenes de navegador adicionales para permitir CORS |

---

### acp

Inicie un servidor ACP (Agent Client Protocol).

```bash
jyycode acp
```

Este comando inicia un servidor ACP que se comunica a través de stdin/stdout usando nd-JSON.

#### Opciones

| Opción                                   | Descripción                  |
| ---------------------------------------- | ---------------------------- |
| <nobr><code>{"--cwd"}</code></nobr>      | Directorio de trabajo        |
| <nobr><code>{"--port"}</code></nobr>     | Puerto para escuchar         |
| <nobr><code>{"--hostname"}</code></nobr> | Nombre de host para escuchar |

---

### uninstall

Desinstale JYYCode y elimine todos los archivos relacionados.

```bash
jyycode uninstall
```

#### Opciones

| Opción                                      | Corta | Descripción                               |
| ------------------------------------------- | ----- | ----------------------------------------- |
| <nobr><code>{"--keep-config"}</code></nobr> | `-c`  | Mantener archivos de configuración        |
| <nobr><code>{"--keep-data"}</code></nobr>   | `-d`  | Conservar datos de sesión e instantáneas  |
| <nobr><code>{"--dry-run"}</code></nobr>     |       | Mostrar lo que se eliminaría sin eliminar |
| <nobr><code>{"--force"}</code></nobr>       | `-f`  | Saltar mensajes de confirmación           |

---

### upgrade

Actualiza jyycode a la última versión o a una versión específica.

```bash
jyycode upgrade [target]
```

Para actualizar a la última versión.

```bash
jyycode upgrade
```

Para actualizar a una versión específica.

```bash
jyycode upgrade v0.1.48
```

#### Opciones

| Opción                                 | Corta | Descripción                                                         |
| -------------------------------------- | ----- | ------------------------------------------------------------------- |
| <nobr><code>{"--method"}</code></nobr> | `-m`  | El método de instalación que se utilizó; curl, npm, pnpm, bun, brew |

---

## Opciones globales

La CLI de JYYCode toma las siguientes banderas globales.

| Opción                                     | Corta | Descripción                                  |
| ------------------------------------------ | ----- | -------------------------------------------- |
| <nobr><code>{"--help"}</code></nobr>       | `-h`  | Mostrar ayuda                                |
| <nobr><code>{"--version"}</code></nobr>    | `-v`  | Número de versión de impresión               |
| <nobr><code>{"--print-logs"}</code></nobr> |       | Imprimir registros en stderr                 |
| <nobr><code>{"--log-level"}</code></nobr>  |       | Nivel de registro (DEBUG, INFO, WARN, ERROR) |

---

## Variables de entorno

JYYCode se puede configurar mediante variables de entorno.

| Variable                              | Type     | Description                                                                     |
| ------------------------------------- | -------- | ------------------------------------------------------------------------------- |
| `JYYCODE_AUTO_SHARE`                 | booleano | Compartir sesiones automáticamente                                              |
| `JYYCODE_GIT_BASH_PATH`              | cadena   | Ruta al ejecutable de Git Bash en Windows                                       |
| `JYYCODE_CONFIG`                     | cadena   | Ruta al archivo de configuración                                                |
| `JYYCODE_CONFIG_DIR`                 | cadena   | Ruta al directorio de configuración                                             |
| `JYYCODE_CONFIG_CONTENT`             | cadena   | Contenido de configuración json en línea                                        |
| `JYYCODE_DISABLE_AUTOUPDATE`         | booleano | Deshabilitar las comprobaciones automáticas de actualizaciones                  |
| `JYYCODE_DISABLE_PRUNE`              | booleano | Deshabilitar la poda de datos antiguos                                          |
| `JYYCODE_DISABLE_TERMINAL_TITLE`     | booleano | Deshabilitar las actualizaciones automáticas de títulos de terminal             |
| `JYYCODE_PERMISSION`                 | cadena   | Configuración de permisos json incorporados                                     |
| `JYYCODE_DISABLE_DEFAULT_PLUGINS`    | booleano | Deshabilitar complementos predeterminados                                       |
| `JYYCODE_DISABLE_LSP_DOWNLOAD`       | booleano | Deshabilitar las descargas automáticas del servidor LSP                         |
| `JYYCODE_ENABLE_EXPERIMENTAL_MODELS` | booleano | Habilitar modelos experimentales                                                |
| `JYYCODE_DISABLE_AUTOCOMPACT`        | booleano | Deshabilitar la compactación automática de contexto                             |
| `JYYCODE_DISABLE_CLAUDE_CODE`        | booleano | Deshabilitar la lectura desde `.claude` (mensaje + habilidades)                 |
| `JYYCODE_DISABLE_CLAUDE_CODE_PROMPT` | booleano | Desactivar lectura `~/.claude/CLAUDE.md`                                        |
| `JYYCODE_DISABLE_CLAUDE_CODE_SKILLS` | booleano | Deshabilitar la carga `.claude/skills`                                          |
| `JYYCODE_DISABLE_MODELS_FETCH`       | booleano | Deshabilitar la recuperación de modelos desde fuentes remotas                   |
| `JYYCODE_FAKE_VCS`                   | cadena   | Proveedor de VCS falso para fines de prueba                                     |
| `JYYCODE_CLIENT`                     | cadena   | Identificador de cliente (por defecto `cli`)                                    |
| `JYYCODE_ENABLE_EXA`                 | booleano | Habilitar las herramientas de búsqueda web de Exa                               |
| `JYYCODE_SERVER_PASSWORD`            | cadena   | Habilite la autenticación básica para `serve`/`web`                             |
| `JYYCODE_SERVER_USERNAME`            | cadena   | Anular el nombre de usuario de autenticación básica (predeterminado `jyycode`) |
| `JYYCODE_MODELS_URL`                 | cadena   | URL personalizada para buscar la configuración de modelos                       |

---

### Experimental

Estas variables de entorno habilitan funciones experimentales que pueden cambiar o eliminarse.

| Variable                                        | Type     | Description                                                |
| ----------------------------------------------- | -------- | ---------------------------------------------------------- |
| `JYYCODE_EXPERIMENTAL`                         | booleano | Habilitar todas las funciones experimentales               |
| `JYYCODE_EXPERIMENTAL_ICON_DISCOVERY`          | booleano | Habilitar descubrimiento de íconos                         |
| `JYYCODE_EXPERIMENTAL_DISABLE_COPY_ON_SELECT`  | booleano | Deshabilitar copia al seleccionar en TUI                   |
| `JYYCODE_EXPERIMENTAL_BASH_DEFAULT_TIMEOUT_MS` | número   | Tiempo de espera predeterminado para comandos bash en ms   |
| `JYYCODE_EXPERIMENTAL_OUTPUT_TOKEN_MAX`        | número   | Tokens de salida máximos para respuestas LLM               |
| `JYYCODE_EXPERIMENTAL_FILEWATCHER`             | booleano | Habilite el observador de archivos para todo el directorio |
| `JYYCODE_EXPERIMENTAL_OXFMT`                   | booleano | Habilitar el formateador oxfmt                             |
| `JYYCODE_EXPERIMENTAL_LSP_TOOL`                | booleano | Habilitar herramienta experimental LSP                     |
| `JYYCODE_EXPERIMENTAL_DISABLE_FILEWATCHER`     | booleano | Deshabilitar el observador de archivos                     |
| `JYYCODE_EXPERIMENTAL_EXA`                     | booleano | Habilitar funciones experimentales de Exa                  |
| `JYYCODE_EXPERIMENTAL_LSP_TY`                  | booleano | Habilitar Habilitar TY LSP para archivos python            |
| `JYYCODE_EXPERIMENTAL_PLAN_MODE`               | booleano | Habilitar modo de plan                                     |
