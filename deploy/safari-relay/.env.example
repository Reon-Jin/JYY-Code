# A DNS A/AAAA record for this name must point to the server before starting.
JYYCODE_PUBLIC_HOST=remote.example.com

# Address used by the desktop companion.  It must exactly match the public host.
JYYCODE_MOBILE_RELAY_URL=wss://remote.example.com/connect
